import React, { Fragment } from 'react';

export const NotFound = () => {
  return (
    <Fragment>
      <h1 className='lead'>Not found page....</h1>
    </Fragment>
  );
};

export default NotFound;
